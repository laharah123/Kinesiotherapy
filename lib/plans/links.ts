/**
 * External links opened from the app.
 *
 * PLACEHOLDER: swap in the real published policy URL before release. It is kept
 * here, in one place, so the Profile screen never links to a dead route.
 */
export const PRIVACY_URL = 'https://example.com/kinesiotherapy/privacy';
